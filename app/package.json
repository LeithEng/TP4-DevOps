{
  "name": "mon-app-devops",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "JEST_JUNIT_OUTPUT_DIR=coverage JEST_JUNIT_OUTPUT_NAME=junit.xml jest --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.3"
  }
}